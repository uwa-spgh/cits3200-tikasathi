# Core Module
This directory contains app-wide shared infrastructure:
- `database/`: Drift SQLite setup and DAOs.
- `theme/`: Colors, typography, and styling.
- `l10n/`: Localization setup (English/Nepali).
- `services/`: Global services like `flutter_local_notifications`.
