import 'package:flutter/material.dart';

class LearnPlaceholderScreen extends StatelessWidget {
  const LearnPlaceholderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Learn placeholder'),
    );
  }
}
